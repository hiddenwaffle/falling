import {Board} from '../board/board';

export class Ai {

    private board: Board;

    constructor(board: Board) {
        this.board = board;
    }

    start() {
        //
    }

    step(elapsed: number) {
        let matrix = this.board.matrix;
    }
}